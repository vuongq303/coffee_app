import 'package:flutter/material.dart';

class ListFutureBuilder extends StatelessWidget {
  const ListFutureBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
